# React-Auth

A production-ready frontend authentication template built with React/Next.js, featuring HttpOnly Cookie authentication, access &amp; refresh token flow, RBAC, permission-based authorization, protected routes, OAuth, and a fully mocked API with MSW.
